# Release status

Current status: **Candidate — E2E implementation complete; provenance-refreshed clean-runtime rerun pending**. The standalone `E2E` tutorial `tutorials/swin_segmentation_colab.ipynb` implements pretrained ADE20K inference plus bounded notebook-local adaptation under DIMER Notebook Specification 2.0. Its default path validates a 24-scene generated raster-mask dataset, uses a disjoint 18/6 split, re-heads the decode and auxiliary heads for three classes, freezes the Swin-T backbone, fine-tunes with AdamW, evaluates mIoU/pixel accuracy/per-class IoU against a majority baseline, exports an adapter and reloads it with exact-mask verification.

GitHub Actions run `34974178073` executed the PR #10 carrier in a scratch CPython 3.10 CPU runtime and passed 16/16 code cells plus the output assertions. That notebook nevertheless carried a stale `generated_from.revision` (`79e83e8`) that did not contain the embedded E2E source modules, so the run is retained as implementation evidence but is not promotion evidence for the provenance-refreshed carrier. Static validation now requires the recorded source revision to contain the exact carried module bytes. The exact refreshed notebook blob still requires a clean supported-runtime run and explicit promotion review.

The separate DIMER composed-worker `GRADIENT-ADAPTATION` surface remains `scaffold` (`spec/pipeline-surface.json`, `scripts/verify_scaffold.py`). Notebook-local adaptation does not satisfy its missing task/representation profiles, validator and finetuner releases, accelerator qualification or release composition.
