# Release status

Current status: **Candidate — standalone tutorial source complete, awaiting clean-runtime execution**. The pretrained task-inference package (`dimer_swin_segmentation`, with the fleet snapshot scheme, `validate_inputs`, `evaluation_report` and the carried `semantic_iou` / `majority_class_baseline` metrics), its offline unit tests, `MODEL_CARD.md`, and the standalone `TASK-INFERENCE` tutorial `tutorials/swin_segmentation_task_inference.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 1.1 §3.6) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). The previous repository-installing notebook (Notebook Specification 1.0) had a recorded clean GitHub-hosted execution on 2026-09-11; that evidence belongs to the replaced carrier and does not transfer. The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit of the standalone notebook is recorded in `docs/release-verification.md`.

The composed-worker gradient-adaptation surface remains `scaffold` (`spec/pipeline-surface.json`, `scripts/verify_scaffold.py`); the pretrained inference status above does not change it.
