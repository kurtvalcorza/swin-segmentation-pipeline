.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
# Pinned OpenMMLab checkpoint downloaded by acquire_verified_checkpoint()
.dimer-models/

# Model weights are pulled locally (see weights/); never commit checkpoints
weights/**/*.pth
